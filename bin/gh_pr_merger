#!/usr/bin/env ruby

require 'gh_pr_merger'

merger = GhPrMerger.new(access_token: ENV['GH_TOKEN'])

exit merger.run(base_repo: ARGV[0], base_branch: ARGV[1], merge_branch: ARGV[2], fork_repo: ARGV[3])